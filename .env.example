# Alpaca paper trading (data + execution)
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_BASE_URL=https://paper-api.alpaca.markets/v2

# NewsAPI.org (headline source for the sentiment factor)
NEWSAPI_KEY=

# LLM used for sentiment scoring (GPT-4o-mini via OpenAI API)
LLM_API_KEY=

# Render Postgres connection string, for live trading logs (orders/fills/snapshots)
DATABASE_URL=

# Comma-separated origins allowed to call the FastAPI backend (backend/main.py).
# Defaults to "*" if unset; set to your Vercel deployment URL(s) once deployed.
ALLOWED_ORIGINS=